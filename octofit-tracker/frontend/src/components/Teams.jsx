import { useApi } from '../api.js'

export default function Teams() { const { items, loading, error } = useApi('teams'); return <section className="collection"><div className="section-heading"><h2>Teams</h2><span>{items.length} squads</span></div>{loading && <p className="status">Loading data...</p>}{error && <p className="status error">{error}</p>}<div className="card-grid">{items.map((team) => <article className="data-card" key={team._id || team.name}><span className="card-kicker">TEAM / {team.memberIds?.length || 0} MEMBERS</span><h3>{team.name}</h3><p>{team.description}</p></article>)}</div></section> }